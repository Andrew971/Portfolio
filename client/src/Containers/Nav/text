<MobileView>
  <TopBar className="row align-items-center justify-content-around">
    <NavLink className="col-10 brand-text" to="/">
      Mybrand
    </NavLink>
    <div className="col-2">
      <select
        onChange={e => {
          dispatch(
            UIAction({type: "CHANGE_LANG", payload: e.target.value})
          );
        }}
        value={Language}
      >
        <option value="En">En</option>
        <option value="Fr">Fr</option>
      </select>
    </div>
  </TopBar>
</MobileView>

<NavBar>
  <MobileView className="row justify-content-around">
    <div
      onClick={() => {
        history.push("/");
      }}
      style={{fontSize: "3rem", color: "white"}}
    >
      <i class="fas fa-briefcase" />
    </div>
    <div
      onClick={() => {
        history.push("/skills");
      }}
      style={{fontSize: "3rem", color: "white"}}
    >
      <i class="fas fa-code" />
    </div>
    <div
      onClick={() => {
        history.push("/about");
      }}
      style={{fontSize: "3rem", color: "white"}}
    >
      <i class="fas fa-user-circle" />
    </div>
  </MobileView>
  <DesktopView className="row justify-content-around">
    <div className="col-2">
      <NavLink className="navbar-brand" to="/" align="">
        Mybrand
      </NavLink>
    </div>
    <div className="col-8">
      <ul>
        <li>
          <NavLink to="/">Portfolio</NavLink>
        </li>
        <li>
          <NavLink to="/skills">Skills</NavLink>
        </li>
        <li>
          <NavLink to="/about">Contact</NavLink>
        </li>
        <select
          onChange={e => {
            dispatch(
              UIAction({type: "CHANGE_LANG", payload: e.target.value})
            );
          }}
          value={Language}
        >
          <option value="En">En</option>
          <option value="Fr">Fr</option>
        </select>
      </ul>
    </div>
  </DesktopView>
</NavBar>



<button className="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
  <span className="navbar-toggler-icon"></span>
</button>

<div className="collapse navbar-collapse" id="navbarTogglerDemo02">
<ul>
  <li>
    <NavLink to="/">Portfolio</NavLink>
  </li>
  <li>
    <NavLink to="/skills">Skills</NavLink>
  </li>
  <li>
    <NavLink to="/about">Contact</NavLink>
  </li>
  <select
    onChange={e => {
      dispatch(
        UIAction({type: "CHANGE_LANG", payload: e.target.value})
      );
    }}
    value={Language}
  >
    <option value="En">En</option>
    <option value="Fr">Fr</option>
  </select>
</ul>
</div>
